# MeasureQual 0.10.0

- Renamed from `evaluably` to `MeasureQual` to reflect claim-scoped measurement qualification.
- Scientific algorithms and the 21-function exported analytical API are unchanged.
- Replace `library(evaluably)` with `library(MeasureQual)` and `evaluably::` with `MeasureQual::`.
- Stable condition identifiers and the frozen schema URN are retained for compatibility.
- Optional `gbm201data` retains its name; patch 0.9.1 updates documentation and package independence checks, with unchanged resource bytes.

# evaluably 0.9.0

First public packaging of the frozen ACT6 V2 implementation dated 2026-09-13.
Scientific R sources, tests, examples, vignettes and companion resource bytes
are unchanged. Public author contact, repository layout and release documentation
are curated separately. Historical internal audit archives are excluded.
