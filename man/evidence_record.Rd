% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/records.R
\name{evidence_record}
\alias{evidence_record}
\title{Emit a Claim-Scoped Evidence Record}
\usage{
evidence_record(
  x,
  ...,
  relationships = list(),
  render = TRUE,
  path = NULL,
  formats = c("json", "html")
)
}
\arguments{
\item{x}{Audit object or evidence JSON path where supported.}

\item{...}{Additional arguments forwarded to the documented next phase.}

\item{relationships}{List of record link identifiers/hashes, never evidence content.}

\item{render}{Whether to write when a path is supplied.}

\item{path}{Optional output directory.}

\item{formats}{Requested output formats; JSON is always written.}
}
\value{
Immutable evidence_record; optionally writes JSON and a human-readable report.
}
\description{
Qualifies a run or blocked check and assembles structured interpretation constraints and dual hashes. Relationships never transfer evidence. Raw patient data are not included.
}
\section{Errors}{

Malformed contracts raise structured evaluably conditions. Verification and
phase mismatches fail closed; no decision rule rescues a blocked gate.
}

\examples{
library(evaluably)
source(system.file("examples", "toy-workflow.R", package = "evaluably"))
t <- toy_spec()
s <- freeze_audit(t$spec)
evidence_record(check_audit(freeze_audit(toy_spec(capacity = FALSE)$spec), t$data))
}
